# کارهای باقی‌مانده (تأیید بصری)

هر چهار موردِ فهرست آخر کد شده‌اند. `npx tsc --noEmit`، `npx next build` و سه اسکریپت
`scripts/check-*.mjs` (۳۷ گزارهٔ دوربین) پاس می‌شوند. چیزی که از این‌جا به بعد
مانده، فقط چیزهایی است که با کد نمی‌شود تأیید کرد و باید چشمی/دستی دیده شود.

## ۱. آزاد شدن دوربین بعد از برداشتن فوکوس
- ریشهٔ اصلی پیدا شد: در حالت فوکوس هر فریم `controls.target` به خودِ جسم
  جوش می‌خورد و **هیچ‌وقت آزاد نمی‌شد**. بعد از برداشتن فوکوس، تراک‌بال دور
  نقطه‌ای می‌چرخید که *روی سطح* بود؛ چرخش کره‌ای می‌ساخت که بیشترش داخل زمین/سیاره
  بود و گارد برخورد هر فریم دوربین را بیرون می‌راند، و زوم هم بلافاصله به گارد
  می‌خورد. یعنی هیچ چیزی دوربین را «قفل» نکرده بود — محورِ چرخش جایی بود که
  فضایی برای حرکت نداشت.
- حالا با `releasePivotRef` + `RELEASE_RATE` محور چرخش به مرکز صحنه برمی‌گردد
  و **موقعیت دوربین دست نمی‌خورد**. `camera.up` هم عمداً دست نخورده می‌ماند.
- **باقی‌مانده:** یک بار در My Fleet ماهواره‌ای را TRACK کن، فوکوس را بردار و ببین
  چرخش/زوم آزاد است؛ و همین را در منظومهٔ شمسی با یک سیاره تکرار کن.

## ۲. حساسیت زوم و تغییر زاویه
- چهار کلاس دستگاه (`phone | tablet | laptop | desktop`) در `lib/interaction.ts`
  هرکدام جدولِ جدا دارند: درجه‌بر‌سوایپ، بهرهٔ زوم، نرخ میرایی، سقف هر فریم و
  dead-zone. مهم‌ترین جداسازی لپ‌تاپ از کامپیوتر است و دلیلش سخت‌افزار ورودی است
  نه اندازهٔ صفحه (تاچ‌پد ده‌ها رویداد ریز در ثانیه می‌فرستد، چرخ موشی چند پرشِ
  ۱۲۰ پیکسلی).
- **پیش‌فرض حالا روی `Standard` است، روی هر دستگاهی.** قبلاً موبایل/تبلت با
  `Calm` باز می‌شد؛ آن کار وقتی معنی داشت که جدول‌ها مشترک بودند. الان
  `Standard` روی موبایل خودش «آرامِ موبایل» است، پس دو بار آرام کردن فقط کُند
  حسش می‌کرد و معنی اسمِ پریست را از دستگاهی به دستگاه دیگر عوض می‌کرد.
- **نمای منظومهٔ شمسی حالا جداگانه کم شده و زمین دست‌نخورده است:** جدول‌ها خودشان
  تنظیمِ زمین‌اند و `VIEW_TRIM` در `lib/interaction.ts` فقط برای منظومهٔ شمسی
  ضریب می‌زند — چرخش ۰٫۵۵ و زوم ۰٫۴۵. دلیلش هندسی است: در نمای زمین محور چرخش
  همان کره‌ای است که نگاه می‌کنی، پس ۷۸ درجه سوایپ دور همان چیز می‌چرخد؛ در
  منظومهٔ شمسی محور معمولاً خورشید یا فضای خالی است و همان ۷۸ درجه سوژه را کامل
  از کادر بیرون می‌برد. زوم هم در منظومهٔ شمسی گامش را با فضای خالی جلوی دوربین
  می‌سنجد (`zoomScale`) که همیشه ≥ فاصلهٔ محور است، پس هر نُچ راه بیشتری می‌رفت.
  سقف‌های هر فریم عمداً دست نخورده‌اند (آن‌ها بازگردانده نمی‌شوند، پس کم کردنشان
  ورودیِ یک سوایپ سریع را می‌خورد، نه این‌که آرامش کند).
- **باقی‌مانده:** حسش کن. اگر جایی «زیاد» یا «کم» بود، فقط بگو کدام‌یک: عدد
  `VIEW_TRIM.solar` (برای منظومهٔ شمسی) یا خانهٔ همان دستگاه از جدول‌های
  `DEGREES_PER_SWIPE` / `ZOOM_GAIN`.

## ۳. داخلیِ سیارات (cutaway)
- کد کامل است: `lib/planet-interiors.ts` + `components/PlanetCutaway.tsx` +
  `components/PlanetInteriorPanel.tsx`. انتخاب لایه فقط با **کلیک** است.
- **اسم سیاره دیگر روی این پنل نمی‌افتد.** برچسب‌های صحنه در واقع DOM هستند
  (drei `Html`)، نه پیکسل داخل WebGL؛ پنل رویشان می‌نشیند ولی پرده‌اش نیمه‌شفاف
  است، پس یک چیپ سفید از زیرش خوانده می‌شد. حالا کلاس `scene-label` روی همهٔ
  برچسب‌ها هست و پنل تا وقتی باز است `scene-labels-hidden` را روی `<body>`
  می‌گذارد (قاعده‌اش در `app/globals.css`). پرده را عمداً مات نکردم — آن ظاهر
  شیشه‌ای را از بین می‌برد و علت را هم درست نمی‌کرد.
- **باقی‌مانده:** دیدن رندر واقعی WebGL و تأیید اینکه ظاهرش واقع‌گرایانه است.

## ۴. مدل سه‌بعدی ماهواره در نمای منظومهٔ شمسی
- کد کامل است: `components/SatelliteModel.tsx`، فقط در `viewMode === 'solar'`.
- **باقی‌مانده:** تأیید اینکه با کلیک روی یک ماهوارهٔ زمین در نمای منظومهٔ شمسی،
  مدل سه‌بعدی مخصوص آن نمایش داده می‌شود.

## ۵. چیدمان تبلت و ریسپانسیو (کف ۳۰۰px)
- **همپوشانیِ نوار پایین حل شد، دو جا:**
  - در چیدمان دسکتاپ (از `lg` بالا — تبلتِ لندسکیپ هم همین‌جاست) فضای رزروشده
    برای نوار پایین دیگر عدد ثابت `pb-32` نیست؛ ارتفاع واقعی نوار با
    `ResizeObserver` اندازه‌گیری و همان‌قدر padding رزرو می‌شود. عدد ثابت برای
    «دو ردیف چیپ» درست بود و برای هر چیز دیگری غلط: چیپ‌ها در هر عرضی تعداد
    ردیفشان عوض می‌شود و HISTORY / SCALE / داکِ منظومه هرکدام یک پنل کامل
    بالای چیپ‌ها اضافه می‌کنند — با HISTORY باز، نوار ~۲۵۰px می‌شد و می‌نشست
    روی کارت جزئیات و کارت دسته‌بندی.
  - ارتفاع کارت‌های ستون چپ از `46vh` اول به درصدِ **ستون** و بعد (در فهرست آخر،
    بخش ۹) به گریدِ `46fr / 54fr` تغییر کرد. عدد `vh` ارتفاعی که نوار پایین گرفته
    را نادیده می‌گرفت و در پنجرهٔ کوتاه تبلت برای فیلترها ~۱۰۰px می‌ماند.
  - در چیدمان کشویی (زیر `lg`) وقتی کارت جزئیات باز است، نوار در تبلت دیگر
    مخفی نمی‌شود بلکه **بالای** کارت می‌رود (`min(430px,62dvh)` — همان ارتفاع
    کارت). روی موبایل همان‌طور کنار می‌رود، چون آن‌جا واقعاً جا نیست.
- مرز چیدمان `lg` (۱۰۲۴px) است؛ تبلت پرتره چیدمان کشویی موبایل را می‌گیرد.
- نوار بالای موبایل: دکمهٔ نیم‌دایره حذف شد و فقط روی ریل پایین است؛ پشت نام
  PIMXSATS هم هیچ کارت/پس‌زمینه‌ای نیست.
- **باقی‌مانده:** یک بار روی تبلت واقعی در **پرتره و لندسکیپ** نگاه کردن — به‌ویژه
  لندسکیپ با HISTORY باز و یک ماهوارهٔ انتخاب‌شده، که همان حالتِ خراب بود.

## ۶. لود شدن Space weather قبل از بالا آمدن سایت
- ریشه: خودِ کارت مالکِ درخواست بود. `SpaceWeather.tsx` روی mount یک
  `requestIdleCallback` می‌گذاشت و آن fetch می‌کرد؛ چون عوض کردن نما کارت را
  unmount/remount می‌کند، **هر بار** ورود به منظومهٔ شمسی یعنی یک تا دو ثانیه
  دیدنِ برچسب LINKING — از جمله اولین بار.
- حالا انتقال داده در `lib/space-weather.ts` است، دقیقاً به شکل
  `preloadCloudTexture` در `lib/clouds.ts`: یک `state` ماژولی + یک promiseِ در
  جریان، پس هر تعداد صدا‌زننده = یک درخواست. `getSpaceWeather()` هم‌زمان
  (synchronous) است، بنابراین کارت **اولین فریمی که می‌کِشد عدد دارد** نه خط تیره.
- `boot()` در `SatelliteApp.tsx` آن را کنار prefetchِ موتور و کامپوزیتِ ابر صدا
  می‌زند. عمداً `await` نمی‌شود: راه‌اندازی قاعده‌اش این است که هیچ API بیرونی روی
  مسیر بحرانی نباشد (کاتالوگ هم به همین دلیل داخل سایت bundle شده)، و گره زدنِ
  نمایش سایت به NOAA کل سایت را به‌کندیِ کندترین سرورِ بالادست می‌کرد تا یک کارت
  درست شود. چیزی که کارت لازم دارد «اولِ صف بودن» است، نه نگه‌داشتنِ splash.
- شکست هم صادقانه است: اگر NOAA جواب ندهد، آخرین payload سالم می‌ماند و فقط
  برچسب به DELAYED / NO LINK می‌رود.
- **باقی‌مانده:** یک بار با شبکهٔ کُند وارد شو و ببین کارت از همان اول عدد دارد.

## ۷. پس‌زمینهٔ سیاهِ Deep Space در کارت Spacecraft
- آن نوار سیاه، هدرِ چسبانِ گروه در `ProbesPanel` بود با `bg-[#05070d]/95` —
  یعنی یک مستطیل تقریباً مات که روی شیشهٔ کارت می‌نشست.
- حالا فقط `backdrop-blur-md` دارد و هیچ رنگِ خودش را نمی‌گذارد: رنگِ خودِ کارت را
  می‌گیرد، ولی اسم‌هایی که از زیرش رد می‌شوند باز هم محو می‌شوند نه خوانا (وگرنه
  دو خط متن روی هم می‌افتاد).

## ۸. قفل شدن صفحه بعد از بستن کارت سیاره در منظومهٔ شمسی
- **ریشه پیدا و اثبات شد (عددی، نه حدس).** دکمه‌های سیاره در `SolarDock` دوربین را
  فوکوس می‌کنند؛ بستنِ کارت `clearSelection` را می‌زند و آن `releasePivotRef` را
  روشن می‌کند، و glideِ آزادسازی محورِ چرخش را به **مبدأ صحنه** می‌بُرد — که در
  منظومهٔ شمسی «خورشید» است، حدود ۱ AU پشتِ دوربینی که چسبیده به سیاره پارک شده.
- در آن ژست، چرخش با `0.25 × clearance / dist` محدود می‌شود (برای عطارد ≈ ۰٫۰۴۵
  درجه در فریم) و زوم‌به‌داخل هم روی گاردِ برخورد گیر است؛ یعنی عملاً فقط
  زوم‌به‌بیرون جواب می‌دهد. حسِ کاربر: صفحه قفل است.
- **راه‌حل درست محورِ چرخش بود، نه سقفِ زاویه.** گذاشتن کف روی سقفِ زاویه غلط بود:
  یک درجه دور خورشید، دوربین را به اندازهٔ شعاع زمین جابه‌جا می‌کند، پس آن محدودیت
  فیزیکاً درست است. کاری که کردم `desiredPivot()` است: مقصدِ آزادسازی — و به‌طور
  پیوسته هر ژستی که `gap / dist < PIVOT_SWING_MIN` باشد — مرکزِ همان جسمی است که
  دوربین کنارش است، و آن‌جا نسبتِ حاکم `gap / (gap + radius)` ≥ ۰٫۲۳ است.
- نمای زمین هیچ تغییری نمی‌کند (مبدأ صحنه *همان* مرکز کره است). هر دو شاخهٔ جدید
  در LEVEL VIEW، OVERVIEW و تورِ خودکار کنار می‌کشند (`!controls.enabled`).
- **در `scripts/check-controls.mjs` گزارهٔ ۱۶ اضافه شد و همان قفل را بازتولید
  می‌کند:** یک سوایپ یکسان روی درگاهِ عطارد، ۱٫۳۴ درجه با محورِ خورشید و ۶٫۴۳ درجه
  با محورِ عطارد. نکتهٔ سنجش: `settle()` قدیمی ۴۰۰ فریم می‌راند و چون سقفِ زاویه
  آن‌چه نگه می‌دارد را **پس می‌دهد**، در ۴۰۰ فریم حتی ژستِ قفل هم کل سوایپ را
  تحویل می‌داد — به همین دلیل اندازه‌گیری روی ~۳۰ فریم (بازهٔ واقعیِ حرکتِ دست)
  انجام می‌شود.
- **باقی‌مانده:** همان مسیر را دستی برو (سیاره از لیست → بستن کارت → چرخاندن).

## ۹. هم‌ارتفاع شدن کارت‌ها
- **دسکتاپ:** هر دو ستون حالا یک گریدِ یکسان‌اند: `grid grid-rows-[46fr_54fr] gap-3`.
  `fr` فضایی را تقسیم می‌کند که **بعد از gap** می‌ماند، پس با ارتفاع ظرفِ یکسان و
  gapِ یکسان، دو عددِ یکسان بیرون می‌آید — کارت SIM TIME دقیقاً هم‌قدِ کارت
  Overhead Alerts / My Fleet، و کارت جزئیات دقیقاً هم‌قدِ Filters / Spacecraft.
  (به همین دلیل gapِ ستون راست از `gap-4` به `gap-3` آمد؛ اگر gap دو ستون یکی
  نباشد، `fr` هم یکی نمی‌شود.) قبلاً هر دو ستون پشته‌ای به ارتفاعِ محتوا بودند و
  آن چهار کارت فقط تصادفی به هم می‌رسیدند.
- `InfoCard` در حالت دسکتاپ از `h-[min(430px,52dvh)] xl:h-[430px]` به `h-full`
  رفت تا ردیفِ گرید ارتفاعش را تعیین کند. عددِ ثابتِ قبلی در پنجرهٔ کوتاه با نوارِ
  عمیق، از ستونی که داخلش بود سرریز می‌کرد.
- **گوشی:** کارت SIM TIME و کارت Overhead Alerts داخل یک `grid grid-rows-2` رفتند.
  در ظرفی که ارتفاع خودش نامعیّن است (همین کشویی که خودش اسکرول می‌کند)، ردیف‌های
  `1fr` مساوی به ارتفاعِ بلندترین محتوا حل می‌شوند: پس هر دو کارت دقیقاً به قدِ
  بلندتری درمی‌آیند و کوتاه‌تر، تفاضل را به‌صورت فضای خالی نگه می‌دارد. نه سقفی
  گذاشته شده و نه چیزی بریده می‌شود — و مهم‌تر، اسکرولِ تو‌در‌توی لمسی درست نشده.
- کارت Filters / Spacecraft در کشویی از `max-h-[70vh]` به `h-[min(430px,62dvh)]`
  رفت، که **همان ارتفاع شیتِ جزئیات** در موبایل است. روی گوشی این دو هیچ‌وقت
  کنار هم نیستند (یکی شیتِ لبهٔ پایین، دیگری داخل کشویی)، پس یکی‌بودنِ عدد تنها
  راهِ هم‌اندازه بودنشان است. ثابت است نه سقف، تا لیست کوتاه (چند فضاپیمای نمای
  منظومه) هم همان کارت را بدهد.
- `CategoriesPanel` هم لازم بود عوض شود: لیستش `max-h-[32vh]` بود، یعنی نسبت به
  **پنجره** نه نسبت به کارت. حالا که ارتفاع کارت سهمی از ستون است، پنجرهٔ کوتاه
  می‌توانست کمتر از آن ۳۲vh جا بدهد و `overflow-hidden` کارت، ردیف‌های آخر را
  می‌بُرید بدون این‌که چیزی بتواند تا آن‌ها اسکرول کند. حالا `flex-1 min-h-0 h-full`
  است و لیست هرچه کارت باقی گذاشته را می‌گیرد — همان شکلِ `ProbesPanel`.
- **هزینهٔ آگاهانه:** هم‌ارتفاع بودن یعنی کارتِ کوتاه‌ترِ هر جفت، ته‌اش فضای خالی
  دارد (مثلاً Space weather در منظومهٔ شمسی مقابل پنلِ کاملِ زمان). این ذاتِ
  «دقیقاً هم‌اندازه» است. اگر ترجیح می‌دهی به‌جای سهمِ ثابت، هم‌ارتفاعی از روی
  محتوا اندازه‌گیری شود (مثل ارتفاع نوار پایین با `ResizeObserver`)، بگو تا عوض کنم.
- **باقی‌مانده:** یک بار روی گوشی و یک بار روی دسکتاپ، در **هر دو نما** نگاه کن.

## ۱۰. ارتفاع زیادِ کارت Space weather
- بعد از هم‌ارتفاع‌سازیِ بخش ۹، کارت بالای ستون چپ در نمای منظومه (که فقط
  Space weather است) کشیده می‌شد تا ۴۶٪ ستون را پر کند — یک پنل کوچک داخل یک
  شیشهٔ بلندِ خالی.
- در دسکتاپ ردیف‌ها همان `46fr / 54fr` مانده (تا کارت پایین با کارت جزئیات
  هم‌تراز بماند) ولی خودِ کارت در نمای منظومه `self-start max-h-full` گرفته، پس
  به‌قدِ محتوایش است و باقیِ ردیف شفاف می‌ماند. در نمای زمین کشیده می‌شود، چون
  آن کارت همان چیزی است که کارت SIM TIME باید اندازه‌اش باشد.
- در کشویی گوشی، در نمای منظومه اصلاً جفت‌سازی انجام نمی‌شود (`space-y` عادی)،
  وگرنه یا کارتِ خالیِ بلند می‌داد یا فاصلهٔ خالیِ بلند وسط چیزی که اسکرول می‌شود.

## ۱۱. قفل شدن کامل صفحه با دکمهٔ OVERVIEW
- **ریشه: خطِ پایانِ آن glide پوزی بود که خودِ کنترلر اجازه نمی‌دهد.**
  `SmoothControls.update()` هر فریم `camera.up` را نسبت به راستای دید عمود
  می‌کند؛ پس دوربینی که از `(0, 5, 10)` به مبدأ نگاه می‌کند up ≈ `(0, 0.89, −0.45)`
  دارد و **هیچ‌وقت** `(0, 1, 0)` نمی‌شود. شرط پایانِ glide دقیقاً همان
  `camera.up ≈ WORLD_UP` بود، یعنی هرگز برقرار نمی‌شد: `solarResetRef` تا آخر
  session روشن می‌ماند و هر فریم دوربین را به `(0, 5, 10)` برمی‌گرداند. حسِ کاربر
  همان است که گفت: تکان می‌دهی، درجا برمی‌گردد سرِ جایش.
- علتِ اینکه در نمای زمین (LEVEL VIEW) این باگ نبود: مقصدِ آن **روی** صفحهٔ
  استوایی است، آن‌جا راستای دید افقی است و upِ عمودشده *همان* world up است.
- حالا مقصد `SOLAR_HOME_UP` است: world up منهای مؤلفه‌اش در راستای دید — همان
  چیزی که کنترلر واقعاً روی آن می‌نشیند. به‌علاوه یک ترمزِ سخت
  (`SOLAR_RESET_MAX_FRAMES = 90` ≈ ۱٫۵ ثانیه) گذاشته شد: دوربینی که کاربر
  فرمانش نمی‌دهد باید حتماً پایانی داشته باشد.
- دو گزارهٔ ۱۷ در `scripts/check-controls.mjs` همین را می‌سنجد و عدد چاپ می‌کند:
  در پوزِ OVERVIEW فاصلهٔ `cam.up` از world up **۰٫۴۶۰** است (پس شرط قدیمی
  دست‌نیافتنی بود) و از upِ تصویرشده **۰٫۰۰۰۰**.

## چرا باقی مانده
رندر WebGL و حس لمسی را نمی‌شود از روی کد تأیید کرد. این موارد نیاز به بازبینی
چشمی خودت دارد.
