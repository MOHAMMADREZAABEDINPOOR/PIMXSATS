# Cloudflare Pages build: @cloudflare/next-on-pages@1 has a peerOptional
# conflict with the wrangler@4 it pulls in (workers-types ^4 vs ^5). npm's
# strict peer resolution aborts the npx install; legacy resolution is safe
# here and matches what next-on-pages' own CI uses.
legacy-peer-deps=true
